# ip-lookup

CLI tool to look up IP address info. No API key needed (uses ipapi.co's free tier).

## Usage
```bash
npm install
npx ts-node src/index.ts              # your own IP
npx ts-node src/index.ts 8.8.8.8     # specific IP
```
